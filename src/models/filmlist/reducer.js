const initialState ={
  filmsList: []
};

const filmsReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default: {
      return state;
    }
  }
};

export default filmsReducer;